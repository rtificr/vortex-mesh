[package]
name = "vortex-mesh"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
glam = "0.33.3"
tobj = "4.0.5"
vortex-project = { path = "../vortex-project" }
